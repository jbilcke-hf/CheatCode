# HuggingFace API Token
# Get your token at: https://huggingface.co/settings/tokens
# Required for Space upload, optional otherwise (lower rate limits without it)
HF_TOKEN=your_huggingface_token_here

# Example: HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# HuggingFace Username
# Your HuggingFace username (required for uploading to Spaces)
# Get it from your profile at: https://huggingface.co/settings/account
HF_USERNAME=your_username_here

# Example: HF_USERNAME=johndoe

# Repository Storage Path
# Where to clone GitHub repositories for analysis
# If not set, uses system temp directory
# Example: REPOS_PATH=./repositories
REPOS_PATH=

# Auto-fetch Papers on Startup
# Set to "true" to automatically fetch papers from HuggingFace when the app starts
# Defaults to "false" for faster startup and better user control
# Example: AUTO_FETCH_ON_STARTUP=true
AUTO_FETCH_ON_STARTUP=false

# Auto-retry Failed Jobs on Startup
# Set to "true" to automatically retry failed processing jobs when the app starts
# Looks for papers with ERROR status and retries incomplete steps
# Defaults to "false" to avoid unexpected processing on startup
# Example: AUTO_RETRY_FAILED=true
AUTO_RETRY_FAILED=false

# Claude CLI Path
# Path to the claude CLI executable for automatic repository initialization
# If not set, will attempt to auto-detect claude in common locations
# Set this if claude is installed in a custom location
# Example: CLAUDE_CLI_PATH=/usr/local/bin/claude
CLAUDE_CLI_PATH=

# Claude CLI Auto-Install
# Automatically install Claude CLI if not found (useful for Docker/production)
# Options: "true" to enable, "false" to disable (default: false)
# Example: CLAUDE_AUTO_INSTALL=true
CLAUDE_AUTO_INSTALL=false

# Claude Installation Method
# Method to use when auto-installing Claude CLI
# Options: "auto" (detect best method), "curl" (use curl script), "skip" (don't install)
# Default: "auto"
# Example: CLAUDE_INSTALL_METHOD=curl
CLAUDE_INSTALL_METHOD=auto

# Anthropic API Key
# API key for Claude CLI authentication (if using API-based auth)
# Get your key at: https://console.anthropic.com/
# Alternative: Use Claude Console login or cloud provider credentials
# Example: ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_API_KEY=

# Claude Auto-Approve
# Automatically approve all Claude CLI operations (skip permission prompts)
# Options: "true" to enable, "false" to disable (default: true for automation)
# WARNING: Only use in trusted/sandboxed environments
# Example: CLAUDE_AUTO_APPROVE=true
CLAUDE_AUTO_APPROVE=true

# Claude Init Enabled
# Enable or disable automatic repository initialization with claude /init
# Set to "false" if you have insufficient API credits or want to skip initialization
# Options: "true" to enable, "false" to disable (default: true)
# Example: CLAUDE_INIT_ENABLED=false
CLAUDE_INIT_ENABLED=true

# Claude Init Timeout
# Maximum time (in seconds) to wait for claude /init to complete
# For large repositories, initialization can take a long time
# Options: numeric value in seconds, or "none"/"infinite" for no timeout
# Default: 1800 (30 minutes)
# Examples:
#   CLAUDE_INIT_TIMEOUT=3600    # 1 hour
#   CLAUDE_INIT_TIMEOUT=7200    # 2 hours
#   CLAUDE_INIT_TIMEOUT=none    # No timeout (wait indefinitely)
CLAUDE_INIT_TIMEOUT=1800

# Space Upload Configuration
# Automatically upload repositories to HuggingFace Spaces after Gradio app generation
# Requires HF_TOKEN and HF_USERNAME to be set

# SPACE_UPLOAD_ENABLED
# Enable or disable automatic Space upload
# If not set, auto-enables when both HF_TOKEN and HF_USERNAME are present
# Options: "true" to enable, "false" to disable
# Example: SPACE_UPLOAD_ENABLED=true
SPACE_UPLOAD_ENABLED=true

# SPACE_UPLOAD_PRIVATE
# Create private Spaces by default (requires paid HuggingFace account)
# Options: "true" for private, "false" for public (default)
# Example: SPACE_UPLOAD_PRIVATE=true
SPACE_UPLOAD_PRIVATE=true

# SPACE_UPLOAD_FORCE
# Force overwrite existing Spaces with the same name during upload
# If set to "false" (default), will skip upload if Space already exists
# If set to "true", will update/overwrite existing Spaces
# Useful when retrying failed uploads or updating existing Spaces
# Options: "true" to force overwrite, "false" to skip existing (default)
# Example: SPACE_UPLOAD_FORCE=true
SPACE_UPLOAD_FORCE=false
